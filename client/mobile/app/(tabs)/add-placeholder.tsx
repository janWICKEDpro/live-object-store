import { Redirect } from 'expo-router';

// This is just a placeholder because we use a custom button in the layout
// that opens a modal directly.
export default function AddPlaceholder() {
    return <Redirect href="/" />;
}
